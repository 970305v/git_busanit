<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.app.mapper.QnaMapper">
   <insert id="qnaWrite">
   INSERT INTO qna(qId, qCategory, pId, mId, qTitle, qContent, qFile, qSecret) VALUES(NULL, #{qCategory}, #{pId}, #{mId}, #{qTitle}, #{qContent}, #{qFile}, #{qSecret})
   </insert>
</mapper>