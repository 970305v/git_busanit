<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.app.mapper.MemberMapper">
   <insert id="insertData">
      INSERT INTO member(mId, mEmail, mPwd, mName, mPhone, mPostnum, mAddr1, mAddr2) VALUES(NULL, #{mEmail}, #{mPwd}, #{mName}, #{mPhone}, #{mPostnum}, #{mAddr1}, #{mAddr2})         
   </insert>
   <select id="selectDataOne" resultType="com.project.app.dto.MemberDTO">
      SELECT * FROM member WHERE mId = #{mId}
   </select>
   <update id="updateData" parameterType="com.project.app.dto.MemberDTO">
      UPDATE member SET title = #{title}, writer = #{writer}, pw = #{pw}, content = #{content} WHERE idx = #{idx}
   </update>
   <delete id="deleteData">
      DELETE FROM member WHERE mId = #{mId}
   </delete>
   <select id="selectDataAll" resultType="com.project.app.dto.MemberDTO">
      SELECT * FROM member
   </select>
   <select id="selectDataCount" resultType="int">
      SELECT COUNT(idx) AS 'count' FROM member
   </select>
   <select id="emailCheck" resultType="int">
      SELECT COUNT(*) AS 'COUNT' FROM member WHERE mEmail = #{mEmail}
   </select>
   <select id="userLogin" resultType="int">
      SELECT COUNT(*) AS 'COUNT' FROM member WHERE mEmail = #{mEmail} AND mPwd = #{mPwd}
   </select>
   <select id="getUserAccount" resultType="String">
      SELECT mPwd FROM member WHERE mEmail= #{mEmail}
    </select>
</mapper>